import React, { FC } from 'react';
import Dashboard from '../components/layout/Dashboard';

const messages: FC = () => {
    return <Dashboard>These are your chats</Dashboard>;
};

export default messages;
